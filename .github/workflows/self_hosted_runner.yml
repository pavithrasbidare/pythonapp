name: Workflow for Self Hosted
run-name: Self Hosted Runner workflow
on:
 push

jobs:
  test-build:
   runs-on: self-hosted
   steps:
   - name: simple test for workflow
     run: |
      echo "this is line1"
      echo "this is line2"

 
